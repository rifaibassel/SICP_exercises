What Eva Lu Ator is referencing is the fact that before the change square was being called on a value once but with the explicit multiplication we evaluate the same call twice resulting in each call branching into two calls, now since before it was taking log n steps and now it branches twice which is an exponential process the exponent and the log cancel out and the process now takes $\Theta(n)$ steps
